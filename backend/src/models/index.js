const User = require('./User.model');
const CreditScore = require('./CreditScore.model');
const Transaction = require('./Transaction.model');
const CreditReport = require('./CreditReport.model');

module.exports = {
  User,
  CreditScore,
  Transaction,
  CreditReport,
};
